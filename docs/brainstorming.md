# 脑暴议题与待决策清单

历史待决策仍然有效，以下为本阶段新增结论与后续问题。

## 本阶段已达成的实现决策

- Phase 1 先做 MySQL 持久化与 GitHub Actions CI/CD，RAG 延后。
- 运行时数据库驱动使用 `asyncmy`，Alembic 迁移使用 `PyMySQL`。
- SQLite 继续保留为测试专用方言，用于无外部依赖的 `pytest`。
- Redis 继续保留在生产 compose 中，但当前不编造业务使用场景。

## 仍待确认

### 1. 企业微信能力确认
- [ ] 自动回复窗口、主动回访权限与频率限制？
- [ ] AES 解密、签名、回调体格式的正式实现细节？
- [ ] 真实发送 API 与失败重试边界？

### 2. RAG 延后边界
- [ ] `knowledge_documents` 何时扩展为 chunk/vector/rerank 结构？
- [ ] 是否需要引入 pgvector / 专用向量库？
- [ ] 文档更新与重建索引的后台任务如何调度？

### 3. 部署与运维
- [ ] 是否需要反向代理（Nginx/Caddy）统一 TLS 与访问控制？
- [ ] 备份频率、保留期、异地容灾目标？
- [ ] schema 变更何时需要引入“向后兼容发布窗口”？
