name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/wecom-ai-customer-service
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=long
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # 服务器使用 SSH 密码登录：密码通过 SSHPASS 环境变量传给 sshpass -e，
      # 不写入命令行参数，也不打印到日志。
      # 临时方案：当前阶段关闭 host key 校验（StrictHostKeyChecking=no，
      # UserKnownHostsFile=/dev/null），因此不再需要 SERVER_KNOWN_HOSTS。
      # 这会带来中间人攻击风险，部署稳定后应恢复 StrictHostKeyChecking=yes
      # 并改用 SSH key + known_hosts。
      - name: Install sshpass
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq sshpass

      - name: Upload compose manifest
        env:
          SSHPASS: ${{ secrets.SERVER_PASSWORD }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_SSH_PORT: ${{ vars.SERVER_SSH_PORT || '22' }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          sshpass -e ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PubkeyAuthentication=no -o PreferredAuthentications=password -p "$SERVER_SSH_PORT" "$SERVER_USER@$SERVER_HOST" "mkdir -p '$DEPLOY_PATH'"
          sshpass -e scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PubkeyAuthentication=no -o PreferredAuthentications=password -P "$SERVER_SSH_PORT" docker-compose.yml "$SERVER_USER@$SERVER_HOST:$DEPLOY_PATH/docker-compose.yml"

      - name: Deploy over SSH
        env:
          SSHPASS: ${{ secrets.SERVER_PASSWORD }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_SSH_PORT: ${{ vars.SERVER_SSH_PORT || '22' }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          IMAGE_REF: ${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          APP_PORT: ${{ vars.APP_PORT || '8000' }}
          APP_ENV: production
          APP_NAME: wecom-ai-assistant
          APP_VERSION: ${{ github.sha }}
          LOG_LEVEL: ${{ vars.LOG_LEVEL || 'INFO' }}
          TZ: ${{ vars.TZ || 'Asia/Shanghai' }}
          DEFAULT_TENANT_SLUG: ${{ vars.DEFAULT_TENANT_SLUG || 'default-tenant' }}
          DEFAULT_TENANT_NAME: ${{ vars.DEFAULT_TENANT_NAME || '默认企业' }}
          MYSQL_DATABASE: ${{ vars.MYSQL_DATABASE || 'wecom_ai' }}
          MYSQL_USER: ${{ vars.MYSQL_USER || 'wecom_ai' }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
          REDIS_URL: redis://redis:6379/0
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL || 'https://api.openai.com/v1' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'gpt-4o-mini' }}
          EMBEDDING_BASE_URL: ${{ vars.EMBEDDING_BASE_URL || 'https://api.openai.com/v1' }}
          EMBEDDING_API_KEY: ${{ secrets.EMBEDDING_API_KEY }}
          EMBEDDING_MODEL: ${{ vars.EMBEDDING_MODEL || 'text-embedding-3-small' }}
          WECOM_CORP_ID: ${{ secrets.WECOM_CORP_ID }}
          WECOM_SECRET: ${{ secrets.WECOM_SECRET }}
          WECOM_TOKEN: ${{ secrets.WECOM_TOKEN }}
          WECOM_AES_KEY: ${{ secrets.WECOM_AES_KEY }}
          HANDOFF_CONFIDENCE_THRESHOLD: ${{ vars.HANDOFF_CONFIDENCE_THRESHOLD || '0.35' }}
          SENSITIVE_KEYWORDS: ${{ vars.SENSITIVE_KEYWORDS || '退款纠纷,投诉,起诉,律师' }}
          RETRIEVAL_TOP_K: ${{ vars.RETRIEVAL_TOP_K || '5' }}
          REVISIT_AUTO_SEND_ENABLED: ${{ vars.REVISIT_AUTO_SEND_ENABLED || 'false' }}
        run: |
          sshpass -e ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PubkeyAuthentication=no -o PreferredAuthentications=password -p "$SERVER_SSH_PORT" "$SERVER_USER@$SERVER_HOST" /bin/sh <<EOF
          set -eu
          mkdir -p "$DEPLOY_PATH"
          cat > "$DEPLOY_PATH/.env" <<ENVFILE
          APP_ENV=$APP_ENV
          APP_NAME=$APP_NAME
          APP_VERSION=$APP_VERSION
          LOG_LEVEL=$LOG_LEVEL
          APP_PORT=$APP_PORT
          TZ=$TZ
          DEFAULT_TENANT_SLUG=$DEFAULT_TENANT_SLUG
          DEFAULT_TENANT_NAME=$DEFAULT_TENANT_NAME
          APP_IMAGE=$IMAGE_REF
          DATABASE_URL=mysql+asyncmy://$MYSQL_USER:$MYSQL_PASSWORD@mysql:3306/$MYSQL_DATABASE?charset=utf8mb4
          REDIS_URL=$REDIS_URL
          MYSQL_DATABASE=$MYSQL_DATABASE
          MYSQL_USER=$MYSQL_USER
          MYSQL_PASSWORD=$MYSQL_PASSWORD
          MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD
          LLM_BASE_URL=$LLM_BASE_URL
          LLM_API_KEY=$LLM_API_KEY
          LLM_MODEL=$LLM_MODEL
          EMBEDDING_BASE_URL=$EMBEDDING_BASE_URL
          EMBEDDING_API_KEY=$EMBEDDING_API_KEY
          EMBEDDING_MODEL=$EMBEDDING_MODEL
          WECOM_CORP_ID=$WECOM_CORP_ID
          WECOM_SECRET=$WECOM_SECRET
          WECOM_TOKEN=$WECOM_TOKEN
          WECOM_AES_KEY=$WECOM_AES_KEY
          HANDOFF_CONFIDENCE_THRESHOLD=$HANDOFF_CONFIDENCE_THRESHOLD
          SENSITIVE_KEYWORDS=$SENSITIVE_KEYWORDS
          RETRIEVAL_TOP_K=$RETRIEVAL_TOP_K
          REVISIT_AUTO_SEND_ENABLED=$REVISIT_AUTO_SEND_ENABLED
          ENVFILE
          cd "$DEPLOY_PATH"
          CURRENT_APP_ID=$(docker compose ps -q app || true)
          if [ -n "$CURRENT_APP_ID" ]; then
            docker inspect --format='{{.Config.Image}}' "$CURRENT_APP_ID" > .previous_app_image || true
          fi
          docker compose pull app migrate
          docker compose --profile ops run --rm migrate
          docker compose up -d app mysql redis
          sleep 10
          curl -fsS "http://127.0.0.1:$APP_PORT/health"
          printf '%s\n' "$IMAGE_REF" > .current_app_image
          EOF
